{
  "name": "@andera-top/base-worker",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -rf dist",
    "dev": "ts-node src/app.ts",
    "start": "node dist/app.js",
    "lint": "prettier --write --check \"**/*.{ts,tsx}\""
  },
  "files": [
    "dist"
  ],
  "license": "Elastic License 2.0",
  "dependencies": {
    "@andera-top/worker-core": "^0.1.1",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "prettier": "^3.5.3",
    "ts-node": "^10.9.2"
  }
}
